name: CD

on:
  push:
    branches: [ master ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    name: Docker Image Build & Push
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout kodi
        uses: actions/checkout@v4

      - name: GitHub Container Registry ga kirish
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata tayyorlash
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Docker BuildKit yoqish
        uses: docker/setup-buildx-action@v3

      - name: Docker image build va push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # -------------------------------------------------------
  # DEPLOY BOSQICHI (ixtiyoriy — serverga moslashtiring)
  # -------------------------------------------------------
  # VPS yoki AWS EC2 ga deploy qilish uchun
  # quyidagi job ni faollashtiring va GitHub Secrets ni sozlang:
  #   SSH_HOST      — server IP manzili
  #   SSH_USERNAME  — SSH foydalanuvchi nomi (masalan: ubuntu)
  #   SSH_KEY       — shaxsiy SSH kalit (-----BEGIN OPENSSH PRIVATE KEY-----)
  #
  # deploy:
  #   name: Serverga Deploy
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   steps:
  #     - name: SSH orqali deploy
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.SSH_HOST }}
  #         username: ${{ secrets.SSH_USERNAME }}
  #         key: ${{ secrets.SSH_KEY }}
  #         script: |
  #           echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
  #           docker pull ghcr.io/${{ github.repository }}:latest
  #           docker stop crm_web || true
  #           docker rm crm_web || true
  #           docker run -d \
  #             --name crm_web \
  #             -p 8080:8080 \
  #             -e ASPNETCORE_ENVIRONMENT=Production \
  #             -e ConnectionStrings__DefaultConnection="${{ secrets.DB_CONNECTION }}" \
  #             --restart unless-stopped \
  #             ghcr.io/${{ github.repository }}:latest
